/* Official website change monitor. Published GitHub data, no browser scraping. */
const CompetitiveIntelligence=(()=>{
 let data=null,loaded=false,view='pages';const $=id=>document.getElementById(id);
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const date=s=>s?new Date(s).toLocaleDateString('en-US',{month:'short',day:'2-digit',year:'numeric'}):'—';
 const safe=u=>{try{return /^https?:$/.test(new URL(u).protocol)}catch{return false}};
 const link=(u,t)=>safe(u)?'<a href="'+esc(u)+'" target="_blank" rel="noopener">'+esc(t)+' ↗</a>':esc(t);
 const image=u=>/^competitive\/images\/[a-f0-9]+\.jpg$/.test(u||'')?'<a href="'+esc(u)+'" target="_blank" rel="noopener"><img src="'+esc(u)+'" loading="lazy" alt="Captured official website"></a>':'<p class="muted">저장된 화면이 없습니다.</p>';
 const badge=s=>'<span class="ci-badge ci-'+esc(s.toLowerCase().replaceAll(' ','-'))+'">'+esc(s)+'</span>';
 function options(id,values){const old=$(id).value;$(id).innerHTML='<option value="">All</option>'+[...new Set(values.filter(Boolean))].sort().map(v=>'<option>'+esc(v)+'</option>').join('');if(values.includes(old))$(id).value=old;}
 function filter(rows){const m=$('ciMolecule').value,b=$('ciBrand').value,q=$('ciQuarter').value,t=$('ciType').value,s=$('ciSearch').value.toLowerCase();return rows.filter(r=>(!m||r.molecule===m)&&(!b||r.brand===b)&&(!t||r.pageType===t)&&(!q||(r.quarter||quarter(r.checkedAt))===q)&&(!s||[r.brand,r.operator,r.title,r.url].join(' ').toLowerCase().includes(s)));}
 function quarter(s){if(!s)return '';return s.slice(0,4)+' Q'+Math.ceil(Number(s.slice(5,7))/3);}
 function stats(){const rows=filter(data.pages),cov=data.coverage.filter(p=>(!$('ciMolecule').value||p.molecule===$('ciMolecule').value)&&(!$('ciBrand').value||p.brand===$('ciBrand').value));$('ciStats').innerHTML=[['Tracked products',cov.filter(p=>p.status==='Tracking').length+' / '+cov.length],['Monitored pages',rows.length],['Changes',filter(data.events).length],['Needs review',rows.filter(p=>p.status==='Needs review').length]].map(([a,b])=>'<div class="ci-stat"><span>'+a+'</span><strong>'+b+'</strong></div>').join('');}
 function table(headers,rows){return '<div class="table-scroll"><table class="data-table"><thead><tr>'+headers.map(h=>'<th>'+h+'</th>').join('')+'</tr></thead><tbody>'+rows.join('')+'</tbody></table></div>';}
 function render(){if(!data)return;stats();document.querySelectorAll('[data-ci-view]').forEach(b=>{b.classList.toggle('active',b.dataset.ciView===view);b.setAttribute('aria-pressed',String(b.dataset.ciView===view));});
  let rows,html='';
  if(view==='coverage'){
   rows=data.coverage.filter(p=>(!$('ciMolecule').value||p.molecule===$('ciMolecule').value)&&(!$('ciBrand').value||p.brand===$('ciBrand').value)&&(! $('ciSearch').value||p.brand.toLowerCase().includes($('ciSearch').value.toLowerCase())));
   html=table(['Product / Molecule','Status','Captured / Pages','Official sites'],rows.map(r=>'<tr><td><strong>'+esc(r.brand)+'</strong><small>'+esc(r.molecule)+' · '+(r.kind==='reference'?'Originator':'Biosimilar')+'</small></td><td>'+badge(r.status)+'</td><td>'+r.successful+' / '+r.pages+'</td><td>'+data.sites.filter(s=>s.brand===r.brand).slice(0,3).map(s=>link(s.url,s.operator)).join('<br>')+(r.status==='Not mapped'?'<span class="muted">공식 제품 URL 확인 필요</span>':'')+'</td></tr>'));
  }else if(view==='events'){
   rows=filter(data.events).sort((a,b)=>b.detectedAt.localeCompare(a.detectedAt));
   html=rows.length?rows.map(r=>'<article class="ci-event"><div class="ci-event-head"><div><span class="ci-kicker">'+esc(r.quarter)+' · '+esc(r.pageType)+'</span><h3>'+esc(r.brand)+' <span>'+esc(r.operator)+'</span></h3>'+link(r.url,'Official page')+'</div><div>'+r.types.map(badge).join(' ')+'<p class="muted">Detected '+date(r.detectedAt)+'</p></div></div><p class="panel-note">비교 기준 '+date(r.previousCheckedAt)+' → '+date(r.detectedAt)+' · 발견일은 사이트 게시일과 다를 수 있습니다.</p><div class="ci-diff"><section><h4>Added <span>+'+r.addedCount+'</span></h4>'+r.added.map(s=>'<p>'+esc(s)+'</p>').join('')+'</section><section><h4>Removed <span>−'+r.removedCount+'</span></h4>'+r.removed.map(s=>'<p>'+esc(s)+'</p>').join('')+'</section></div>'+resourceList('Added links / resources',r.linksAdded)+resourceList('Removed links / resources',r.linksRemoved)+resourceList('Updated PDF files',r.filesChanged)+'<details><summary>이전·현재 화면 비교</summary><div class="ci-shots"><section><h4>Previous</h4>'+image(r.before)+'</section><section><h4>Current</h4>'+image(r.after)+'</section></div></details></article>').join(''):'<div class="ci-empty"><h3>아직 감지된 변경이 없습니다.</h3><p>첫 수집은 Baseline으로 저장합니다. 다음 수집부터 이전 기준본과 비교한 변경이 여기에 누적됩니다.</p></div>';
  }else{
   rows=filter(data.pages).filter(r=>!$('ciStatus').value||r.status===$('ciStatus').value).sort((a,b)=>(a.status==='Changed'?-1:b.status==='Changed'?1:0)||a.brand.localeCompare(b.brand));
   html=rows.length?table(['Product / Site operator','Page','Status','Last checked','Evidence'],rows.map(r=>'<tr><td><strong>'+esc(r.brand)+'</strong><small>'+esc(r.molecule)+' · '+esc(r.operator)+'</small></td><td>'+link(r.url,r.title||r.url)+'<small>'+esc(r.pageType)+(r.depth?' · Subpage':' · Entry page')+'</small></td><td>'+badge(r.status)+(r.error?'<small>'+esc(r.error)+'</small>':'')+'</td><td>'+date(r.checkedAt)+'<small>Last success: '+date(r.lastSuccess)+'</small></td><td><button type="button" class="ci-evidence" data-ci-page="'+esc(r.id)+'">근거 보기</button></td></tr>')):'<div class="ci-empty">선택 조건에 해당하는 페이지가 없습니다. Product coverage에서 공식 URL 등록 상태를 확인하세요.</div>';
  }
  $('ciResults').innerHTML=html;$('ciResultCount').textContent=rows.length+' '+(view==='coverage'?'products':view==='events'?'changes':'pages');$('ciStatusWrap').hidden=view!=='pages';$('ciQuarterWrap').hidden=view==='coverage';$('ciTypeWrap').hidden=view==='coverage';
 }
 function resourceList(title,rows){return rows?.length?'<details><summary>'+title+' ('+rows.length+')</summary><ul>'+rows.map(r=>'<li>'+link(r.url,r.label||r.url)+'</li>').join('')+'</ul></details>':'';}
 function evidence(id){const r=data.pages.find(p=>p.id===id);if(!r)return;$('ciDetailTitle').textContent=r.brand+' · '+r.pageType;$('ciDetailBody').innerHTML='<p>'+link(r.url,'Official page')+' · '+link(r.evidence,'Official source / provenance')+'</p><p>Checked '+date(r.checkedAt)+' · First captured '+date(r.firstSeen)+'</p><p>'+badge(r.status)+' '+esc(r.error||'')+'</p><p class="panel-note">'+(r.scopeLimited?'탐색 한도에 도달했습니다. URL watchlist에서 제품별 한도를 늘리거나 중요한 세부 URL을 직접 등록할 수 있습니다.':'')+' 화면은 페이지 상단 최대 2,400px 기준입니다. 텍스트·링크 비교는 수집된 본문을 기준으로 합니다.</p><div class="ci-shots"><section><h4>Previous</h4>'+image(r.previousScreenshot)+'</section><section><h4>Current / last successful</h4>'+image(r.currentScreenshot)+'</section></div>';$('ciDetail').showModal();}
 async function load(){ $('ciFreshness').textContent='게시 데이터를 불러오는 중입니다…';try{const r=await fetch('competitive/index.json',{cache:'no-store'});if(!r.ok)throw Error('아직 수집 게시본이 없거나 불러오지 못했습니다.');const d=await r.json();if(d.schemaVersion!==1)throw Error('지원하지 않는 데이터 형식입니다.');data=d;options('ciMolecule',d.coverage.map(p=>p.molecule));options('ciBrand',d.coverage.map(p=>p.brand));options('ciQuarter',[...d.events.map(p=>p.quarter),...d.pages.map(p=>quarter(p.checkedAt))]);options('ciType',d.pages.map(p=>p.pageType));$('ciFreshness').textContent='Last checked: '+date(d.checkedAt)+' · '+d.schedule;$('ciScope').textContent='Purple Book '+d.coverage.length+'개 브랜드 · 제품당 최대 '+d.maxPagesPerProduct+'페이지 / 깊이 '+d.maxDepth+'단계 · 공식 링크에서 발견한 페이지만 수집 · 수집 실패는 변경 없음으로 처리하지 않습니다.';$('ciSources').innerHTML=d.catalogs.map(s=>'<p>'+link(s.url,s.operator)+' '+badge(s.status)+(s.error?' <small>'+esc(s.error)+'</small>':'')+'</p>').join('');render();}catch(e){$('ciFreshness').textContent=e.message;}}
 function open(){if(loaded)return;loaded=true;$('ciRefresh').onclick=load;document.querySelectorAll('[data-ci-view]').forEach(b=>b.onclick=()=>{view=b.dataset.ciView;render()});['ciBrand','ciQuarter','ciType','ciStatus'].forEach(id=>$(id).onchange=render);$('ciMolecule').onchange=()=>{options('ciBrand',data.coverage.filter(p=>!$('ciMolecule').value||p.molecule===$('ciMolecule').value).map(p=>p.brand));render()};$('ciSearch').oninput=render;$('ciReset').onclick=()=>{['ciMolecule','ciBrand','ciQuarter','ciType','ciStatus','ciSearch'].forEach(id=>$(id).value='');options('ciBrand',data.coverage.map(p=>p.brand));render()};$('ciResults').onclick=e=>{const b=e.target.closest('[data-ci-page]');if(b)evidence(b.dataset.ciPage)};$('ciClose').onclick=()=>$('ciDetail').close();load();}
 return {open};
})();
